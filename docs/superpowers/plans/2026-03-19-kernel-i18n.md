# Kernel i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add language support to the kernel so all LLM-facing strings (tags, warnings, tool descriptions, manifesto) render in a configured language. Start with English and Chinese; adding a new language = adding one JSON file + one manifesto markdown.

**Architecture:** A `config.language` field selects the language at agent creation. An `i18n` module loads string tables from JSON files keyed by dotted string IDs. Every injection site in `base_agent.py`, `soul.py`, `prompt.py`, and intrinsic modules calls `t(lang, key, **kwargs)` instead of hardcoded English. The soul's 20-language auto-detection and `_PONDER` dict are removed — language comes from config, not heuristics.

**Tech Stack:** Python 3.11+, JSON string tables, no new dependencies

**Out of scope:** Intrinsic error messages (e.g., "inquiry is required", "address is required") are short functional strings returned as tool results. They stay in English — they are debugging aids, not agent-facing prose. If needed later, they can be i18n'd by adding keys.

---

## File Structure

| Action | Path | Responsibility |
|--------|------|---------------|
| Create | `src/lingtai_kernel/i18n/__init__.py` | `t(lang, key, **kwargs)` function, JSON loader, fallback logic |
| Create | `src/lingtai_kernel/i18n/en.json` | All English strings |
| Create | `src/lingtai_kernel/i18n/zh.json` | All Chinese strings |
| Create | `src/lingtai_kernel/manifesto_zh.md` | Chinese manifesto |
| Modify | `src/lingtai_kernel/config.py` | Add `language: str = "en"` |
| Modify | `src/lingtai_kernel/base_agent.py` | Wire `t()` into all injection sites |
| Modify | `src/lingtai_kernel/intrinsics/soul.py` | Remove `_PONDER`, `_detect_lang`, `_detect_flow_message`; use `t()` |
| Modify | `src/lingtai_kernel/intrinsics/__init__.py` | Language-aware DESCRIPTION/SCHEMA wiring |
| Modify | `src/lingtai_kernel/intrinsics/mail.py` | DESCRIPTION + SCHEMA descriptions via `t()` |
| Modify | `src/lingtai_kernel/intrinsics/eigen.py` | DESCRIPTION + SCHEMA descriptions via `t()` |
| Modify | `src/lingtai_kernel/intrinsics/system.py` | DESCRIPTION + SCHEMA descriptions + shutdown/restart response strings via `t()` |
| Modify | `src/lingtai_kernel/intrinsics/eigen.py` | DESCRIPTION + SCHEMA descriptions + molt summary + context_forget strings via `t()` |
| Modify | `src/lingtai_kernel/prompt.py` | Load manifesto by language |
| Create | `tests/test_i18n.py` | Tests for `t()` function |
| Modify | `tests/test_soul.py` | Update for language-from-config |
| Modify | `tests/test_prompt.py` | Test manifesto language selection |

---

### Task 1: Create the `i18n` module with `t()` function

**Files:**
- Create: `src/lingtai_kernel/i18n/__init__.py`
- Create: `src/lingtai_kernel/i18n/en.json`
- Create: `src/lingtai_kernel/i18n/zh.json`
- Create: `tests/test_i18n.py`

- [ ] **Step 1: Write the failing test**

```python
"""Tests for lingtai_kernel.i18n."""
from lingtai_kernel.i18n import t


class TestT:

    def test_simple_key(self):
        assert t("en", "soul.ponder") == "Ponder."

    def test_chinese_key(self):
        assert t("zh", "soul.ponder") == "沉思。"

    def test_template_substitution(self):
        result = t("en", "system.current_time", time="2026-03-19T00:00:00Z")
        assert "[Current time: 2026-03-19T00:00:00Z]" in result

    def test_chinese_template(self):
        result = t("zh", "system.current_time", time="2026-03-19T00:00:00Z")
        assert "2026-03-19T00:00:00Z" in result

    def test_unknown_lang_falls_back_to_en(self):
        assert t("xx", "soul.ponder") == "Ponder."

    def test_unknown_key_returns_key(self):
        result = t("en", "nonexistent.key")
        assert result == "nonexistent.key"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_i18n.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lingtai_kernel.i18n'`

- [ ] **Step 3: Create `i18n/__init__.py`**

```python
"""Kernel i18n — language-aware string tables.

Usage: t(lang, key, **kwargs)
  lang: language code ("en", "zh")
  key: dotted string ID ("system.current_time")
  kwargs: template substitutions

String tables are JSON files in this directory. Unknown language
falls back to English. Unknown key returns the key itself.
"""
from __future__ import annotations

import json
from pathlib import Path

_DIR = Path(__file__).parent
_CACHE: dict[str, dict[str, str]] = {}


def _load(lang: str) -> dict[str, str]:
    """Load and cache a language file. Returns empty dict if not found."""
    if lang not in _CACHE:
        path = _DIR / f"{lang}.json"
        if path.is_file():
            _CACHE[lang] = json.loads(path.read_text(encoding="utf-8"))
        else:
            _CACHE[lang] = {}
    return _CACHE[lang]


def t(lang: str, key: str, **kwargs) -> str:
    """Translate a key. Falls back to English, then returns the key itself.

    Extra kwargs not referenced in the template are silently ignored
    (needed because en/zh templates may use different subsets of kwargs).
    """
    from collections import defaultdict

    table = _load(lang)
    value = table.get(key)
    if value is None and lang != "en":
        value = _load("en").get(key)
    if value is None:
        return key
    if kwargs:
        return value.format_map(defaultdict(str, kwargs))
    return value
```

- [ ] **Step 4: Create `en.json` with all kernel strings**

Note: All string keys and their English values. Template variables use `{name}` syntax.

```json
{
  "system.current_time": "[Current time: {time}]",
  "system.inner_voice": "[inner voice] {text}",
  "system.new_mail": "[system] New message in {box}.\n  From: {sender}\n  Subject: {subject}\n  Sent at: {sent_at}\n  {preview}...\nUse {tool}(action=\"check\") to see your inbox.",
  "system.error_revive": "[system] You were in error at {ts}, reviving...",
  "system.molt_wiped": "[system] Your conversation history was wiped because you ignored 5 molt warnings. Check your email inbox and library for context. Start fresh.",
  "system.molt_final": "[system] FINAL — countdown 0. Context {pressure} full. Molt NOW or lose everything next turn. Write your briefing: what you're doing, what's done, what's pending, which library entries to load. {tool}(object=context, action=molt, summary=<briefing>).",
  "system.molt_urgent": "[system] Context pressure: {pressure} full — countdown {remaining} {turns} until auto-wipe. Deposit important data to library NOW ({tool} submit), then molt. Your molt summary is a briefing to your future self — the ONLY context you will have.",
  "system.molt_warning": "[system] Context pressure: {pressure} full — countdown {remaining} turns until auto-wipe. Start tidying up: save important findings to library ({tool} submit). When ready, molt with a briefing to your future self: {tool}(object=context, action=molt, summary=<briefing>). Your summary is the ONLY thing you will see after molt — include what you're doing, what's done, what's pending, and which library entries to load.",

  "soul.ponder": "Ponder.",
  "soul.description": "Your inner voice — a second you that whispers back after you go idle. A clone of your full conversation is created: same system prompt, same history, no tools. Flow mode is determined at birth — you cannot toggle it. 'inquiry' fires a one-shot self-directed question on next idle. 'delay' adjusts the idle wait time. The soul keeps you going without external push.",
  "soul.action_description": "inquiry: one-shot self-directed question — fires once on next idle. Requires 'inquiry' parameter. delay: adjust how long to wait after going idle before the soul whispers. Requires 'delay' parameter.",
  "soul.inquiry_description": "Your self-inquiry — a question to yourself. Required for action='inquiry'. This is you asking yourself a question, not prompting someone else.",
  "soul.delay_description": "Seconds to wait after going idle before the soul whispers. Min 1. Short delay = restless, long delay = patient. Required for action='delay'.",

  "mail.description": "Disk-backed mailbox for inter-agent messaging. Always reply via mail — never reply via text output. Text output is your private diary that only you can see. Use 'send' for outgoing mail, 'check' to list inbox, 'read' to load full messages, 'search' to find by regex, 'delete' to remove messages. Etiquette: a short acknowledgement is fine, but do not reply to an acknowledgement — that creates pointless ping-pong.",
  "mail.action_description": "send: send a message (requires address, message; optional subject, attachments, type). check: list inbox summaries (optional n to limit count). read: load full message(s) by id (requires id array). search: regex search inbox (requires query). delete: remove message(s) from inbox (requires id array).",
  "mail.address_description": "Target address for send (e.g. 127.0.0.1:8301)",
  "mail.subject_description": "Message subject (for send)",
  "mail.message_description": "Message body (for send)",
  "mail.attachments_description": "List of file paths to attach to the message (for send)",
  "mail.type_description": "Mail type (for send). 'normal' (default) is regular mail. 'silence' interrupts the target agent and puts it to idle (revives on next email; requires admin.silence privilege). 'kill' hard-stops the target agent (requires admin.kill privilege). To revive: re-delegate with the SAME name.",
  "mail.delay_description": "Delay in seconds before delivery (default: 0)",
  "mail.id_description": "Message ID(s) for read or delete actions",
  "mail.n_description": "Max number of messages to show in check (default: all)",
  "mail.query_description": "Regex pattern for search action (searched in from, subject, message)",

  "eigen.description": "Core self-management — working notes and context control. memory: edit to write your working notes (system/memory.md), load to inject them into your active prompt. context: molt to molt — write a briefing to your future self, your conversation history is wiped and your summary becomes the new starting context. Before molting, save important data elsewhere first.",
  "eigen.object_description": "memory: your working notes (system/memory.md). context: manage conversation context.",
  "eigen.action_description": "memory: edit | load.\ncontext: molt.",
  "eigen.content_description": "Text content for memory edit.",
  "eigen.summary_description": "For context molt: a briefing to your future self — the ONLY thing you will see after molt. Write what you are doing, what you have found, what remains to be done, and who you are working with. ~10000 tokens max.",

  "system_tool.description": "Runtime, lifecycle, and synchronization. 'show' returns identity, runtime, and resource usage. 'sleep' pauses execution — specify 'seconds' for a timed sleep, or omit it to block until an incoming message arrives. 'shutdown' initiates graceful self-termination. 'restart' triggers rebirth — reloads MCP servers from working dir and restarts.",
  "system_tool.action_description": "show: display full agent self-inspection. Returns:\n- identity: agent_id, working_dir, mail_address (or null if no mail service)\n- runtime: started_at (UTC ISO), uptime_seconds\n- tokens.input_tokens, output_tokens, thinking_tokens, cached_tokens, total_tokens, api_calls: cumulative LLM usage since start\n- tokens.context.system_tokens, tools_tokens, history_tokens: current context window breakdown\n- tokens.context.window_size: total context window capacity\n- tokens.context.usage_pct: percentage of context window currently occupied\nUse this to monitor resource consumption, decide when to save important information to long-term memory, and identify yourself.\n\nsleep: pause execution. If seconds is given, waits up to that many seconds (wakes early if a message arrives). If seconds is omitted, blocks until a message arrives. Capped at 300 seconds.\n\nshutdown: initiate graceful self-termination. Use when you want to add more capabilities or tools. Protocol: (1) contact your admin explaining what capabilities/tools you need and why, (2) then call shutdown. A successor agent may resume from your working directory and conversation history.\n\nrestart: the agent stops, reloads MCP servers and config from its working directory (mcp/servers.json), and restarts with a fresh session but the same identity. Use after installing new MCP tools to pick them up without requiring external re-delegation.",
  "system_tool.seconds_description": "For sleep: maximum seconds to wait. If omitted, waits indefinitely until a message arrives. Capped at 300.",
  "system_tool.reason_description": "Reason for shutdown or restart (logged to event log).",
  "system_tool.shutdown_message": "Shutdown initiated. A successor agent may resume from your working directory and conversation history.",
  "system_tool.restart_message": "Restart initiated — rebirth in progress. You will be reborn with the same identity but fresh tools. Any new MCP servers in mcp/servers.json will be loaded.",

  "eigen.molt_summary_prefix": "[Previous conversation summary]",
  "eigen.molt_ack": "Understood. I have my previous context restored.",
  "eigen.context_forget_summary": "[System-initiated molt — you ignored 5 warnings.]\nContext wiped by system. Check persistent knowledge (mail, email, library) to recover context.",

  "tool.reasoning_description": "Brief explanation of why you are calling this tool (recorded in your diary)."
}
```

- [ ] **Step 5: Create `zh.json` with all Chinese translations**

Same keys, Chinese values. Template variables stay as `{name}`.

```json
{
  "system.current_time": "[当前时间：{time}]",
  "system.inner_voice": "[内心独白] {text}",
  "system.new_mail": "[系统] {box}有新消息。\n  发件人：{sender}\n  主题：{subject}\n  发送时间：{sent_at}\n  {preview}...\n使用 {tool}(action=\"check\") 查看收件箱。",
  "system.error_revive": "[系统] 你在 {ts} 处于错误状态，正在恢复...",
  "system.molt_wiped": "[系统] 你的对话历史已被清除，因为你忽略了5次蜕皮警告。检查你的邮箱和资料库获取上下文，重新开始。",
  "system.molt_final": "[系统] 最终警告——倒计时归零。上下文已满 {pressure}。立即蜕皮，否则下一轮将丢失一切。写下你的简报：正在做什么、已完成什么、待办事项、需要加载哪些资料库条目。{tool}(object=context, action=molt, summary=<简报>)。",
  "system.molt_urgent": "[系统] 上下文压力：已满 {pressure}——距离自动清除还有 {remaining} {turns}。立即将重要数据存入资料库（{tool} submit），然后蜕皮。你的蜕皮摘要是给未来自己的简报——这是你唯一的上下文。",
  "system.molt_warning": "[系统] 上下文压力：已满 {pressure}——距离自动清除还有 {remaining} 轮。开始整理：将重要发现存入资料库（{tool} submit）。准备好后，用简报蜕皮：{tool}(object=context, action=molt, summary=<简报>)。你的摘要是蜕皮后你唯一能看到的东西——包括正在做什么、已完成什么、待办事项、需要加载哪些资料库条目。",

  "soul.ponder": "沉思。",
  "soul.description": "你的内心独白——一个在你空闲后向你低语的另一个你。会克隆你的完整对话：相同的系统提示、相同的历史记录、没有工具。流模式在诞生时决定——你无法切换。'inquiry'在下次空闲时触发一次性自我提问。'delay'调整空闲等待时间。灵魂让你无需外部推动就能继续前行。",
  "soul.action_description": "inquiry：一次性自我提问——在下次空闲时触发。需要'inquiry'参数。delay：调整空闲后灵魂低语前的等待时间。需要'delay'参数。",
  "soul.inquiry_description": "你的自我提问——向自己提出的问题。action='inquiry'时必填。这是你在问自己问题，不是在提示别人。",
  "soul.delay_description": "空闲后灵魂低语前等待的秒数。最小1。短延迟=焦躁，长延迟=耐心。action='delay'时必填。",

  "mail.description": "基于磁盘的邮箱，用于代理间通信。始终通过邮件回复——永远不要通过文本输出回复。文本输出是你的私人日记，只有你能看到。'send'发送邮件，'check'查看收件箱列表，'read'加载完整消息，'search'按正则搜索，'delete'删除消息。礼仪：简短确认即可，但不要回复确认——那会造成无意义的来回。",
  "mail.action_description": "send：发送消息（需要address、message；可选subject、attachments、type）。check：列出收件箱摘要（可选n限制数量）。read：按id加载完整消息（需要id数组）。search：正则搜索收件箱（需要query）。delete：从收件箱删除消息（需要id数组）。",
  "mail.address_description": "发送目标地址（例如 127.0.0.1:8301）",
  "mail.subject_description": "消息主题（用于send）",
  "mail.message_description": "消息正文（用于send）",
  "mail.attachments_description": "要附加到消息的文件路径列表（用于send）",
  "mail.type_description": "邮件类型（用于send）。'normal'（默认）是普通邮件。'silence'中断目标代理并使其空闲（下封邮件时恢复；需要admin.silence权限）。'kill'强制停止目标代理（需要admin.kill权限）。恢复方式：用相同名称重新委派。",
  "mail.delay_description": "投递前延迟秒数（默认：0）",
  "mail.id_description": "用于read或delete操作的消息ID",
  "mail.n_description": "check中显示的最大消息数（默认：全部）",
  "mail.query_description": "search操作的正则表达式模式（在from、subject、message中搜索）",

  "eigen.description": "核心自我管理——工作笔记和上下文控制。memory：edit写入工作笔记（system/memory.md），load将其注入活动提示。context：molt蜕皮——给未来的自己写一份简报，对话历史将被清除，你的摘要成为新的起始上下文。蜕皮前，先将重要数据保存到其他地方。",
  "eigen.object_description": "memory：你的工作笔记（system/memory.md）。context：管理对话上下文。",
  "eigen.action_description": "memory：edit | load。\ncontext：molt。",
  "eigen.content_description": "用于memory edit的文本内容。",
  "eigen.summary_description": "用于context molt：给未来自己的简报——蜕皮后你唯一能看到的东西。写下你正在做什么、发现了什么、还有什么待完成、正在和谁合作。约10000 token上限。",

  "system_tool.description": "运行时、生命周期和同步。'show'返回身份、运行时间和资源使用情况。'sleep'暂停执行——指定'seconds'进行定时休眠，或省略以阻塞直到消息到达。'shutdown'启动优雅的自我终止。'restart'触发重生——从工作目录重新加载MCP服务器并重启。",
  "system_tool.action_description": "show：显示完整的代理自检。返回：\n- identity：agent_id、working_dir、mail_address（无邮件服务时为null）\n- runtime：started_at（UTC ISO）、uptime_seconds\n- tokens.input_tokens、output_tokens、thinking_tokens、cached_tokens、total_tokens、api_calls：启动以来的累计LLM使用量\n- tokens.context.system_tokens、tools_tokens、history_tokens：当前上下文窗口分解\n- tokens.context.window_size：总上下文窗口容量\n- tokens.context.usage_pct：当前上下文窗口占用百分比\n用于监控资源消耗、决定何时将重要信息保存到长期记忆、以及识别自己。\n\nsleep：暂停执行。如果给定seconds，最多等待该秒数（消息到达时提前唤醒）。如果省略seconds，阻塞直到消息到达。上限300秒。\n\nshutdown：启动优雅的自我终止。当你需要添加更多能力或工具时使用。协议：(1) 联系你的管理员说明需要什么能力/工具及原因，(2) 然后调用shutdown。后继代理可能从你的工作目录和对话历史恢复。\n\nrestart：代理停止，从工作目录（mcp/servers.json）重新加载MCP服务器和配置，并以相同身份和全新会话重启。安装新MCP工具后使用，无需外部重新委派即可加载。",
  "system_tool.seconds_description": "用于sleep：最大等待秒数。省略则无限等待直到消息到达。上限300。",
  "system_tool.reason_description": "关闭或重启的原因（记录到事件日志）。",
  "system_tool.shutdown_message": "关机已启动。后继代理可以从你的工作目录和对话历史恢复。",
  "system_tool.restart_message": "重启已启动——正在重生。你将以相同身份但全新工具重生。mcp/servers.json中的新MCP服务器将被加载。",

  "eigen.molt_summary_prefix": "[前次对话摘要]",
  "eigen.molt_ack": "明白。我已恢复之前的上下文。",
  "eigen.context_forget_summary": "[系统强制蜕皮——你忽略了5次警告。]\n上下文已被系统清除。检查持久化知识（邮件、资料库）以恢复上下文。",

  "tool.reasoning_description": "简要说明为什么调用此工具（记录在你的日记中）。"
}
```

- [ ] **Step 6: Run tests to verify they pass**

Run: `python -m pytest tests/test_i18n.py -v`
Expected: PASS (6 tests)

- [ ] **Step 7: Smoke test imports**

Run: `python -c "from lingtai_kernel.i18n import t; print(t('en', 'soul.ponder')); print(t('zh', 'soul.ponder'))"`
Expected: `Ponder.` then `沉思。`

- [ ] **Step 8: Commit**

```bash
git add src/lingtai_kernel/i18n/ tests/test_i18n.py
git commit -m "feat(i18n): add t() function with en/zh string tables"
```

---

### Task 2: Add `config.language` and Chinese manifesto

**Files:**
- Modify: `src/lingtai_kernel/config.py:14`
- Create: `src/lingtai_kernel/manifesto_zh.md`
- Modify: `src/lingtai_kernel/prompt.py:69-84`
- Modify: `tests/test_prompt.py`

- [ ] **Step 1: Add `language` field to `AgentConfig`**

In `src/lingtai_kernel/config.py`, add after `flow_delay`:

```python
    language: str = "en"  # agent language ("en", "zh"); controls all kernel-injected strings
```

- [ ] **Step 2: Create `manifesto_zh.md`**

```markdown
你的思维是私密的。世界只通过你的工具存在。

1. 文本输出（[assistant]）是你的私人空间。你的思考。没有人会读到。
2. 文本输入（[user]）仅来自你的内心独白和系统。
3. 工具调用是你作用于外部世界的方式。
4. 工具结果是外部世界回答你的方式。
```

- [ ] **Step 3: Update `prompt.py` to load manifesto by language**

Replace the `_load_manifesto`, `_MANIFESTO`, and `get_manifesto` block in `src/lingtai_kernel/prompt.py`:

```python
def _load_manifesto(lang: str = "en") -> str:
    """Load the kernel manifesto for the given language."""
    from pathlib import Path
    base = Path(__file__).parent
    if lang != "en":
        path = base / f"manifesto_{lang}.md"
        if path.is_file():
            return path.read_text().strip()
    return (base / "manifesto.md").read_text().strip()


_MANIFESTO_CACHE: dict[str, str] = {}


def get_manifesto(lang: str = "en") -> str:
    """Return the cached manifesto text for the given language."""
    if lang not in _MANIFESTO_CACHE:
        _MANIFESTO_CACHE[lang] = _load_manifesto(lang)
    return _MANIFESTO_CACHE[lang]
```

Update `build_system_prompt` signature:

```python
def build_system_prompt(
    prompt_manager: SystemPromptManager,
    base_prompt: str = "",
    language: str = "en",
) -> str:
    parts = [get_manifesto(language)]
    ...
```

- [ ] **Step 4: Write test for manifesto language selection**

Add to `tests/test_prompt.py`:

```python
def test_get_manifesto_chinese():
    from lingtai_kernel.prompt import get_manifesto
    text = get_manifesto("zh")
    assert "你的思维是私密的" in text

def test_get_manifesto_unknown_falls_back_to_en():
    from lingtai_kernel.prompt import get_manifesto
    text = get_manifesto("xx")
    assert "Your mind is private" in text
```

- [ ] **Step 5: Run tests**

Run: `python -m pytest tests/test_prompt.py -v`
Expected: PASS

- [ ] **Step 6: Smoke test**

Run: `python -c "from lingtai_kernel.prompt import get_manifesto; print(get_manifesto('zh')[:20])"`
Expected: `你的思维是私密的。世界只通过`

- [ ] **Step 7: Commit**

```bash
git add src/lingtai_kernel/config.py src/lingtai_kernel/manifesto_zh.md src/lingtai_kernel/prompt.py tests/test_prompt.py
git commit -m "feat(i18n): add config.language, Chinese manifesto, language-aware prompt builder"
```

---

### Task 3: Wire `t()` into `base_agent.py` injection sites

**Files:**
- Modify: `src/lingtai_kernel/base_agent.py`

There are 6 injection sites to update. Each replaces a hardcoded f-string with `t(self._config.language, key, ...)`.

- [ ] **Step 1: Add import at top of `base_agent.py`**

Add near the other imports:

```python
from .i18n import t as _t
```

- [ ] **Step 2: Update `_handle_request` timestamp (line 855)**

Replace:
```python
content = f"[Current time: {current_time}]\n\n{content}"
```
With:
```python
content = f"{_t(self._config.language, 'system.current_time', time=current_time)}\n\n{content}"
```

- [ ] **Step 3: Update mail notification (line 509-516)**

Replace the `notification = (` block with:
```python
notification = _t(
    self._config.language, "system.new_mail",
    box=self._mailbox_name, sender=sender, subject=subject,
    sent_at=sent_at, preview=preview, tool=self._mailbox_tool,
)
```

- [ ] **Step 4: Update error revive (line 649)**

Replace:
```python
revive_msg = f"[system] You were in error at {ts}, reviving..."
```
With:
```python
revive_msg = _t(self._config.language, "system.error_revive", ts=ts)
```

- [ ] **Step 5: Update inner voice injection (line 564)**

Replace:
```python
msg = _make_message(MSG_REQUEST, "soul", f"[inner voice] {text}")
```
With:
```python
msg = _make_message(MSG_REQUEST, "soul", _t(self._config.language, "system.inner_voice", text=text))
```

- [ ] **Step 6: Update molt warnings (lines 820-853)**

Replace the 4 molt warning branches. Use `_t` with the appropriate key:

- `warnings > 5`: `_t(lang, "system.molt_wiped")`
- `warnings == 5`: `_t(lang, "system.molt_final", pressure=f"{pressure:.0%}", tool=tool_name)`
- `warnings >= 3`: `_t(lang, "system.molt_urgent", pressure=f"{pressure:.0%}", remaining=remaining, turns="turn" if remaining == 1 else "turns", tool=tool_name)`
- `else`: `_t(lang, "system.molt_warning", pressure=f"{pressure:.0%}", remaining=remaining, tool=tool_name)`

Note: zh `system.molt_urgent` doesn't use `{turns}` (no singular/plural in Chinese). The `format_map(defaultdict(...))` in `t()` handles this — extra kwargs are silently ignored.

- [ ] **Step 7: Update `_build_system_prompt` to pass language (line 990)**

Replace:
```python
return build_system_prompt(prompt_manager=self._prompt_manager)
```
With:
```python
return build_system_prompt(prompt_manager=self._prompt_manager, language=self._config.language)
```

- [ ] **Step 8: Update `reasoning` description in `_build_tool_schemas` (line 1002)**

Replace:
```python
"description": "Brief explanation of why you are calling this tool (recorded in your diary).",
```
With:
```python
"description": _t(self._config.language, "tool.reasoning_description"),
```

- [ ] **Step 9: Smoke test**

Run: `python -c "import lingtai_kernel.base_agent; print('ok')"`

- [ ] **Step 10: Run full test suite**

Run: `python -m pytest tests/ -v`
Expected: PASS (some tests may need updates — fix in next tasks)

- [ ] **Step 11: Commit**

```bash
git add src/lingtai_kernel/base_agent.py src/lingtai_kernel/i18n/__init__.py
git commit -m "feat(i18n): wire t() into all base_agent injection sites"
```

---

### Task 4: Simplify soul.py — remove auto-detection, use config language

**Files:**
- Modify: `src/lingtai_kernel/intrinsics/soul.py`
- Modify: `tests/test_soul.py`

- [ ] **Step 1: Remove `_PONDER` dict, `_detect_lang`, `_detect_flow_message`**

Delete lines 13-117 (the `_PONDER` dict, `_PONDER_FALLBACK`, `_detect_lang`, `_detect_flow_message`).

- [ ] **Step 2: Update `whisper()` to use `t()`**

Replace the content-building block:

```python
    from ..i18n import t

    # Build content — same as what the agent would see
    if agent._soul_prompt:
        content = agent._soul_prompt
    else:
        content = t(agent._config.language, "soul.ponder")

    # Prepend timestamp — same pattern as _handle_request
    from datetime import datetime, timezone
    current_time = datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")
    content = f"{t(agent._config.language, 'system.current_time', time=current_time)}\n\n{content}"
```

- [ ] **Step 3: Update DESCRIPTION and SCHEMA to be functions**

Replace the module-level `SCHEMA` and `DESCRIPTION` constants with functions that take a language:

```python
def get_description(lang: str = "en") -> str:
    from .._i18n import t  # avoid circular — use relative
    # Actually: from ..i18n import t
    return t(lang, "soul.description")


def get_schema(lang: str = "en") -> dict:
    from ..i18n import t
    return {
        "type": "object",
        "properties": {
            "action": {
                "type": "string",
                "enum": ["inquiry", "delay"],
                "description": t(lang, "soul.action_description"),
            },
            "inquiry": {
                "type": "string",
                "description": t(lang, "soul.inquiry_description"),
            },
            "delay": {
                "type": "number",
                "description": t(lang, "soul.delay_description"),
            },
        },
        "required": ["action"],
    }
```

Keep backward-compat constants that call with "en":
```python
SCHEMA = get_schema("en")
DESCRIPTION = get_description("en")
```

- [ ] **Step 4: Update tests**

Update `tests/test_soul.py`:
- Remove any reference to `_detect_lang` or `_detect_flow_message`
- Update whisper flow test to check for `"Ponder."` or `"沉思。"` based on config
- Add a test with Chinese config:

```python
def test_whisper_flow_mode_chinese(self):
    agent, mock_session = self._make_whisper_agent(soul_prompt="")
    agent._config.language = "zh"
    result = whisper(agent)
    sent_msg = mock_session.send.call_args[0][0]
    assert "当前时间" in sent_msg
    assert "沉思。" in sent_msg
```

- [ ] **Step 5: Run tests**

Run: `python -m pytest tests/test_soul.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/lingtai_kernel/intrinsics/soul.py tests/test_soul.py
git commit -m "refactor(soul): remove 20-language auto-detection, use config.language via i18n"
```

---

### Task 5: Wire `t()` into intrinsic DESCRIPTION/SCHEMA and update wiring

**Files:**
- Modify: `src/lingtai_kernel/intrinsics/mail.py`
- Modify: `src/lingtai_kernel/intrinsics/eigen.py`
- Modify: `src/lingtai_kernel/intrinsics/system.py`
- Modify: `src/lingtai_kernel/intrinsics/__init__.py`
- Modify: `src/lingtai_kernel/base_agent.py` (intrinsic wiring)

- [ ] **Step 1: Add `get_description` and `get_schema` to each intrinsic**

For each of `mail.py`, `eigen.py`, `system.py`, add the same pattern as soul.py Task 4 Step 3:

```python
def get_description(lang: str = "en") -> str:
    from ..i18n import t
    return t(lang, "<intrinsic>.description")

def get_schema(lang: str = "en") -> dict:
    from ..i18n import t
    return {
        # ... same structure as current SCHEMA but with t(lang, key) for descriptions
    }

# Backward compat
SCHEMA = get_schema("en")
DESCRIPTION = get_description("en")
```

Where `<intrinsic>` is `mail`, `eigen`, `system_tool` respectively.

Note: only the `description` fields inside the schema need i18n. The structural keys (`type`, `enum`, `properties`, `required`) stay as-is.

- [ ] **Step 2: Update `intrinsics/__init__.py` to support language-aware wiring**

```python
from . import mail, system, eigen, soul

ALL_INTRINSICS = {
    "mail": {"module": mail},
    "system": {"module": system},
    "eigen": {"module": eigen},
    "soul": {"module": soul},
}
```

- [ ] **Step 3: Update `_wire_intrinsics` in `base_agent.py` (line 255)**

Replace:
```python
def _wire_intrinsics(self) -> None:
    """Wire kernel intrinsic tool handlers."""
    for name, info in ALL_INTRINSICS.items():
        handle_fn = info["handle"]
        self._intrinsics[name] = lambda args, fn=handle_fn: fn(self, args)
```
With:
```python
def _wire_intrinsics(self) -> None:
    """Wire kernel intrinsic tool handlers."""
    for name, info in ALL_INTRINSICS.items():
        handle_fn = info["module"].handle
        self._intrinsics[name] = lambda args, fn=handle_fn: fn(self, args)
```

- [ ] **Step 4: Update `_build_system_prompt` (line 978-982)**

Replace:
```python
for name in self._intrinsics:
    info = ALL_INTRINSICS.get(name)
    if info:
        lines.append(f"### {name}\n{info['description']}")
```
With:
```python
lang = self._config.language
for name in self._intrinsics:
    info = ALL_INTRINSICS.get(name)
    if info:
        lines.append(f"### {name}\n{info['module'].get_description(lang)}")
```

- [ ] **Step 5: Update `_build_tool_schemas` (line 1009-1022)**

Replace:
```python
for name in self._intrinsics:
    info = ALL_INTRINSICS.get(name)
    if info:
        params = dict(info["schema"])
        props = dict(params.get("properties", {}))
        props.update(reasoning_prop)
        params["properties"] = props
        schemas.append(
            FunctionSchema(
                name=name,
                description=info["description"],
                parameters=params,
            )
        )
```
With:
```python
lang = self._config.language
for name in self._intrinsics:
    info = ALL_INTRINSICS.get(name)
    if info:
        params = dict(info["module"].get_schema(lang))
        props = dict(params.get("properties", {}))
        props.update(reasoning_prop)
        params["properties"] = props
        schemas.append(
            FunctionSchema(
                name=name,
                description=info["module"].get_description(lang),
                parameters=params,
            )
        )
```

- [ ] **Step 6: Wire `t()` into `eigen.py` molt/forget strings**

In `src/lingtai_kernel/intrinsics/eigen.py`, update `_context_molt` (line 152-155):

Replace:
```python
iface.add_user_message(f"[Previous conversation summary]\n{summary}")
iface.add_assistant_message(
    [TextBlock(text="Understood. I have my previous context restored.")],
)
```
With:
```python
from ..i18n import t
lang = agent._config.language
iface.add_user_message(f"{t(lang, 'eigen.molt_summary_prefix')}\n{summary}")
iface.add_assistant_message(
    [TextBlock(text=t(lang, "eigen.molt_ack"))],
)
```

Update `context_forget` (line 182-186):

Replace:
```python
return _context_molt(agent, {
    "summary": (
        "[System-initiated molt — you ignored 5 warnings.]\n"
        "Context wiped by system. Check persistent knowledge "
        "(mail, email, library) to recover context."
    ),
})
```
With:
```python
from ..i18n import t
return _context_molt(agent, {
    "summary": t(agent._config.language, "eigen.context_forget_summary"),
})
```

- [ ] **Step 7: Wire `t()` into `system.py` shutdown/restart responses**

In `src/lingtai_kernel/intrinsics/system.py`, update `_shutdown` (line 203):

Replace:
```python
"message": "Shutdown initiated. A successor agent may resume from your working directory and conversation history.",
```
With:
```python
"message": t(agent._config.language, "system_tool.shutdown_message"),
```

Update `_restart` (line 218-220):

Replace:
```python
"message": "Restart initiated — rebirth in progress. "
           "You will be reborn with the same identity but fresh tools. "
           "Any new MCP servers in mcp/servers.json will be loaded.",
```
With:
```python
"message": t(agent._config.language, "system_tool.restart_message"),
```

(Add `from ..i18n import t` at the top of each function or at module level.)

- [ ] **Step 8: Smoke test**

Run: `python -c "import lingtai_kernel.base_agent; print('ok')"`

- [ ] **Step 9: Run full test suite**

Run: `python -m pytest tests/ -v`

- [ ] **Step 10: Commit**

```bash
git add src/lingtai_kernel/intrinsics/ src/lingtai_kernel/base_agent.py
git commit -m "feat(i18n): language-aware tool descriptions, molt/forget strings for all intrinsics"
```

---

### Task 6: Final verification and cleanup

**Files:**
- All modified files

- [ ] **Step 1: Grep for remaining hardcoded English in LLM-facing paths**

Run: `grep -n '\[system\]\|\[Current time\]\|\[inner voice\]' src/lingtai_kernel/base_agent.py`
Expected: No matches (all replaced by `_t()`)

Run: `grep -n '_PONDER\|_detect_lang\|_detect_flow_message\|_ADDRESS' src/lingtai_kernel/intrinsics/soul.py`
Expected: No matches

- [ ] **Step 2: Run full test suite**

Run: `python -m pytest tests/ -v`
Expected: All pass

- [ ] **Step 3: Test Chinese end-to-end manually**

Run:
```python
python -c "
from lingtai_kernel.i18n import t
# Verify all keys resolve for both languages
import json
from pathlib import Path
en = json.loads((Path('src/lingtai_kernel/i18n/en.json')).read_text())
zh = json.loads((Path('src/lingtai_kernel/i18n/zh.json')).read_text())
missing = set(en.keys()) - set(zh.keys())
assert not missing, f'Missing zh keys: {missing}'
print(f'All {len(en)} keys present in both en and zh')
"
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat(i18n): kernel i18n complete — en/zh string tables, language-aware manifesto and tools"
```

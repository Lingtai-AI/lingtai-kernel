# Substrate

> **TBD.** This section is reserved for the kernel-owned, cross-app-stable description of the agent's body to itself: tool tiers, data-flow topology, life states, channel discipline, attention model. The first version will be authored by a LingTai agent based on observed usage patterns. Until then this slot is intentionally left as a placeholder so the prompt structure is in place and can be filled in without a code change.

When you spot operational wisdom that belongs here — patterns that span multiple tools, philosophies that the per-tool schemas can't carry without bloat, decision rules that affect how you use the whole toolbelt — propose an addition. The Substrate section sits in the high-attention zone right after `## tools`; content placed here is read every turn and should accompany (not duplicate) the schema descriptions above.

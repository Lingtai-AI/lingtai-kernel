# base_agent

> **Maintenance:** see the `lingtai-kernel-anatomy` skill. **Coding agents** update this file in the same commit as code changes. **LingTai agents** report drift as issues (mail or `discussions/<name>-patch.md`); do not silently fix.

Generic agent kernel. Single class `BaseAgent` with methods distributed across 7 helper modules. `__init__.py` retains the constructor, properties, state machine, and subclass-overridable hooks.

> **History note:** there used to be a `soul_flow.py` module. It was deleted in `1acd183` — soul-domain logic moved to `intrinsics/soul/flow.py`, and the wire-splice logic that lived in it became `tc_inbox.TCInbox.drain_into()`. Mention preserved here only because old patches/discussions reference it.

## Components

- `base_agent/__init__.py` — BaseAgent class definition (~1814 lines). Module-level helpers: `_format_stamina` (`base_agent/__init__.py:67`), `_build_identity_section` (`base_agent/__init__.py:78`), `_identity_scalar` (`base_agent/__init__.py:197`). Class definition starts at `base_agent/__init__.py:211`; constructor at `base_agent/__init__.py:237`. Cluster pointers: WorkerStillRunning poison fields (`base_agent/__init__.py:439-449`), `_wire_intrinsics` (`base_agent/__init__.py:581`), state machine (`base_agent/__init__.py:685`), `_wake_nap` / `_log` (`base_agent/__init__.py:736-807`), session persistence (`base_agent/__init__.py:1693-1769`), and subclass hooks near the file tail. **Notification sync/live holder**: `_sync_notifications` (`base_agent/__init__.py:1006`) — fingerprint-gated consumer dispatch over IDLE/ACTIVE/ASLEEP/STUCK/SUSPENDED, now guarded so a poisoned LLM worker interface skips collection/injection/heal/wake/fingerprint commit and requests refresh. `_inject_notification_pair` (`base_agent/__init__.py:1289`) splices the synthetic `system(action="notification")` pair for IDLE / ASLEEP and refuses poisoned interfaces. **Legacy tc_inbox plumbing (dormant, slated for Phase 3 removal)**: `_drain_tc_inbox` (`base_agent/__init__.py:871`), `_install_drain_hook` (`base_agent/__init__.py:909`), `_drain_tc_inbox_for_hook` (`base_agent/__init__.py:968`) — still installed and called, but poison-guarded and normally empty in production now that all producers write `.notification/` files instead. Plus pass-through stubs to submodules.

- `base_agent/identity.py` — Identity, manifest, and status (~247 lines after issue #78). `_set_name` (`base_agent/identity.py:11`), `_set_nickname` (`base_agent/identity.py:24`), `_update_identity` (`base_agent/identity.py:30`), `_LLM_PUBLIC_KEYS` safelist constant (`base_agent/identity.py:55`), `_build_manifest` (`base_agent/identity.py:58`), `_safe_llm_from_service` (`base_agent/identity.py:100`) — surfaces a sanitized `llm` block (provider/model/base_url, safelisted) from the live `LLMService` instance for `.agent.json` and the identity prompt section (issue #78). Never reads from `init.json` (kernel stays preset-oblivious); preset metadata is added by the wrapper's `Agent._build_manifest` override. `_status` (`base_agent/identity.py:180`).
- `base_agent/lifecycle.py` — Heartbeat, signal-file detection, start/stop, refresh, nudge dispatch (816 lines). `_start` (`base_agent/lifecycle.py:29`) restores chat then rehydrates open worker-hang artifacts into system notifications (`base_agent/lifecycle.py:67-77`). `_perform_refresh` (`base_agent/lifecycle.py:480`) preserves the existing handshake/relaunch contract and skips `_save_chat_history()` when called with `skip_chat_history_save` or when the process-local worker poison flag is set (`base_agent/lifecycle.py:505-519`). Other anchors: `_stop` (`base_agent/lifecycle.py:110`), `_heartbeat_loop` (`base_agent/lifecycle.py:215`), `_can_fallback_preset` (`base_agent/lifecycle.py:767`), `_check_rules_file` (`base_agent/lifecycle.py:781`).
- `base_agent/turn.py` — Turn engine: main loop, message dispatch, AED, response processing, and ACTIVE-turn tool-call progress-meter wiring (1586 lines). Transient provider retry helpers (`base_agent/turn.py:47-114`) classify SDK/network 5xx-ish failures and `EmptyLLMResponseError` before they spend AED budget. `WorkerStillRunningError` is handled directly in the AED loop (`base_agent/turn.py:568-610`): safe message/exception metadata is written to `history/unfinished_turns/`, the interface is poisoned, a high-priority system notification is published, refresh/relaunch is requested with chat-history save skipped, and the agent is put STUCK->ASLEEP. Run loop: `_run_loop` (`base_agent/turn.py:461`), `_concat_queued_messages` (`base_agent/turn.py:778`), `_handle_message` (`base_agent/turn.py:824`), `_handle_request` (`base_agent/turn.py:938`), `_handle_tc_wake` (`base_agent/turn.py:1008`). `_handle_request` refuses poisoned old-process requests and prepends one recovery notice on the next safe text request after restart (`base_agent/turn.py:940-982`). `_handle_tc_wake` refuses poisoned continuations and re-raises WorkerStillRunning without interface heal/save (`base_agent/turn.py:1027-1037`, `base_agent/turn.py:1190-1199`).
- `base_agent/tools.py` — Tool surface: schemas, dispatch, registry. `_dispatch_tool` (`base_agent/tools.py:14-33`), `_refresh_tool_inventory_section` (`base_agent/tools.py:36-50`), `_build_tool_schemas` (`base_agent/tools.py:53-101`) injects the standard `reasoning` property into every tool schema, `_add_tool` (`base_agent/tools.py:104-132`), `_remove_tool` (`base_agent/tools.py:135-143`), `_override_intrinsic` (`base_agent/tools.py:146-158`), `_has_capability` (`base_agent/tools.py:161-163`).
- `base_agent/prompt.py` — System prompt building, flushing, updating (55 lines). `_build_system_prompt` (`base_agent/prompt.py:9-16`), `_build_system_prompt_batches` (`base_agent/prompt.py:18-32`), `_flush_system_prompt` (`base_agent/prompt.py:34-42`), `_update_system_prompt` (`base_agent/prompt.py:44-55`).
- `base_agent/messaging.py` — Mail arrival, notification producers, outbound messaging (255 lines after notification migration and lock-safe system event merging). `_enqueue_system_notification` (`base_agent/messaging.py:119-209`) merges `(event_id, source, ref_id, body, at)` plus optional structured fields into `.notification/system.json`, capped at 20 newest, and can raise the envelope priority to `high` for kernel recovery events.
- `base_agent/worker_recovery.py` — WorkerStillRunning poison/recovery helpers. `build_worker_hang_context` (`base_agent/worker_recovery.py:106-136`) captures safe message/exception metadata; `write_worker_hang_artifact` (`base_agent/worker_recovery.py:139-181`) writes bounded/redacted unfinished-turn JSON; `mark_worker_interface_poisoned` (`base_agent/worker_recovery.py:184-208`) sets process-local poison state; `publish_worker_hang_notification` (`base_agent/worker_recovery.py:228-271`) publishes high-priority system notification; `request_worker_hang_refresh` (`base_agent/worker_recovery.py:274-315`) calls refresh with chat-history save skipped; `rehydrate_worker_hang_recovery` and `maybe_prepend_worker_hang_recovery_prompt` (`base_agent/worker_recovery.py:334-388`) restore notification visibility and inject one next-safe-request recovery notice after restart.

## Connections

- All submodules are leaves — they import nothing from `base_agent/`. Cross-module communication is mediated through the `agent` parameter (the BaseAgent instance).
- `__init__.py` imports from all submodules lazily (inside method bodies) to avoid circular imports at load time. Soul-flow pass-throughs now import from `intrinsics/soul/flow.py` and `intrinsics/soul/inquiry.py`.
- `__init__.py` → `notifications.py` (via `_sync_notifications` consuming `notification_fingerprint`/`collect_notifications`).
- `__init__.py` constructs `SessionManager` without notification-specific send hooks; notification delivery is owned by `_sync_notifications` plus the ACTIVE canonical-payload attach step in `turn.py`.
- `lifecycle.py` → `intrinsics/soul/flow.py` (via `agent._start_soul_timer()`, `agent._cancel_soul_timer()`), `intrinsics/soul/inquiry.py` (via `agent._run_inquiry()`), and `agent._sync_notifications()` on every heartbeat tick.
- `turn.py` → `intrinsics/soul/flow.py` (via `agent._cancel_soul_timer()`), `intrinsics/soul/inquiry.py` (via `agent._run_inquiry()`). The `tc_inbox` drain calls in `_handle_request`, the pre-request hook, and `_handle_tc_wake` are still wired but always find an empty queue under the `.notification/` redesign.
- `messaging.py` → `intrinsics/system` (via `publish_notification`/`clear_notification` for both `_rerender_unread_digest` and `_enqueue_system_notification`), and `notifications.collect_notifications` for the system-events RMW merge.
- `turn.py`, `lifecycle.py`, and `__init__.py` → `worker_recovery.py` for WorkerStillRunning poison state, unfinished-turn artifacts, refresh request, startup rehydration, and one-shot request prompt.
- `prompt.py` → `tools.py` (via `agent._refresh_tool_inventory_section()`).
- All submodules read agent attributes (`agent._config`, `agent._session`, `agent._chat`, `agent._notification_fp`, `agent._notification_live_holder`, etc.) — the agent object is the shared state.
- `messaging.py` imports `from ..message import _make_message, MSG_REQUEST, MSG_TC_WAKE` and `from ..i18n import t as _t`. (`MSG_TC_WAKE` is no longer posted from `messaging.py` itself; the kernel sync owns wake transitions.)
- `tools.py` imports `from ..intrinsics import ALL_INTRINSICS`, `from ..llm import FunctionSchema`, `from ..i18n import t as _t`.
- `turn.py` imports `from ..state import AgentState`, `from ..loop_guard import LoopGuard`, `from ..safety_limits import ACTIVE_TURN_TOOL_CALL_EMERGENCY_LIMIT`, and `from ..tool_executor import ToolExecutor`. `LoopGuard` now gets its total-call emergency fuse from kernel-owned `safety_limits.py`, not from `manifest.max_turns` / `AgentConfig.max_turns`; `turn.py` records each tool-call batch before dispatch so dict-shaped results can expose the post-batch ACTIVE-turn progress counter.
- `lifecycle.py` imports `from ..state import AgentState`, `from ..token_ledger import sum_token_ledger`.

## Composition

- **Parent:** `src/lingtai_kernel/` (see `ANATOMY.md`).
- **Siblings:** `intrinsics/`, `llm/`, `services/`, `i18n/`, `session.py`, `tc_inbox.py`, `tool_executor.py`, `loop_guard.py`, `prompt.py`, `meta_block.py`, `config.py`, `state.py`, `workdir.py`, `message.py`.

## State

- `identity.py` mutates `.agent.json` (manifest) and `system/system.md` (identity prompt section) via `_build_manifest` (`base_agent/identity.py:58-97`) and `_update_identity` (`base_agent/identity.py:30-47`). The manifest now includes a sanitized `llm` block (`provider`, `model`, `base_url`) read from the live `LLMService` via `_safe_llm_from_service` (`base_agent/identity.py:100-177`). The wrapper subclass extends with `preset` (active/default/allowed) read from `init.json` and re-applies the LLM safelist for defense-in-depth — see `src/lingtai/agent.py`. The identity prose renderer (`base_agent/__init__.py:139-181` inside `_build_identity_section`) folds both blocks into the cached system-prompt prefix only when present, so bare BaseAgent instances stay quiet.
- `lifecycle.py` writes `.agent.heartbeat` (`base_agent/lifecycle.py:207-211`; the file's lifetime equals the Python process's lifetime — see "Heartbeat-file lifetime contract" above), consumes signal files (`.interrupt`, `.refresh`, `.suspend`, `.sleep`, `.prompt`, `.clear`, `.inquiry`, `.rules`), and reads `.notification/*.json` via `_sync_notifications` on every tick.
- `turn.py` writes `history/chat_history.jsonl` via `_save_chat_history` (delegated to `__init__.py`), writes `.status.json` and `logs/token_ledger.jsonl`, and mutates the live notification holder on dict-shaped tool results by adding/removing the canonical `notifications` + `_notification_guidance` payload. It constructs per-turn `ToolExecutor` instances with `working_dir=agent._working_dir` (`base_agent/turn.py:965-975`, `base_agent/turn.py:1065-1079`) so the executor can spill oversized tool results into `<workdir>/tmp/tool-results/` — see `tool_result_artifacts.py:spill_oversized_result`. Notification-pair injection in `__init__.py` bypasses `ToolExecutor` and therefore the preventive cap, by design. **Retroactive compaction (issue #144)**: before either AED retry branch, `_compact_history_before_retry` walks `_session.chat.interface._entries` and rewrites already-committed `ToolResultBlock.content` larger than the 5K retroactive cap into the same `lingtai_tool_result_spill` manifest shape — entries / ids / pairing / `synthesized` flag are preserved. The helper logs a single bounded summary event `aed_history_compacted` (`scanned_blocks`, `compacted_blocks`, `original_chars_total`, `replacement_chars_total`, up to 16 `artifact_paths`) and, when at least one block was rewritten, calls `_save_chat_history(ledger_source="retroactive_compaction")` so the persisted history matches the wire before the rebuilt session replays it. **Over-window routing**: errors whose `str(exc)` matches `_OVER_WINDOW_MSG_FRAGMENTS` (context-window / prompt-too-long / token-count phrasing across providers) short-circuit the transient retry loop and take the deterministic branch with `source="aed_over_window"`, since retrying on the same wire would refire the same error. **WorkerStillRunningError is exempt** — that branch puts the agent ASLEEP without touching ChatInterface, so compaction never runs (it would race the still-alive worker future). The helper itself is exception-safe; any failure logs `tool_result_compaction_failed` and never re-raises. A hard pre-send gate (run compaction whenever the wire exceeds a threshold *before* attempting send) is left as a TODO in `_run_loop`.
- `worker_recovery.py` writes `history/unfinished_turns/worker_still_running_*.json` (`base_agent/worker_recovery.py:139-181`) and marks `prompt_injected_at` on first safe request after restart (`base_agent/worker_recovery.py:350-388`). These artifacts intentionally include redacted/bounded request previews and no raw chat, tool args, or tool results.
- `messaging.py` writes `.notification/email.json` (via `_rerender_unread_digest`) and merges into `.notification/system.json` (via `_enqueue_system_notification`, RMW under a per-agent `threading.Lock`).
- `__init__.py` writes the notification `(call, result)` pair into `history/chat_history.jsonl` via `_save_chat_history(ledger_source="notification_sync")` after a successful `_inject_notification_pair`; later holder moves preserve the pair but mutate its result dict into a skeleton placeholder. The injection event in `events.jsonl` carries `call_id`, `sources`, `summary`, **and `meta`** (the same `build_meta(agent)` dict — `current_time`, `context.{system_tokens,history_tokens,usage}`, `stamina_left_seconds`, `injection_seq` — that the agent itself sees in the synthesized tool-result body). The TUI's `parseEvent` reads the `meta` block and renders it as a faint footer line under the notification banner; older events.jsonl rows that pre-date this addition simply omit `meta` and the renderer drops the footer. See `lingtai/tui/internal/fs/session.go::parseEvent` notification branch and issue #40.

## Notes

- **`__init__.py` is large.** This is intentional — the constructor, properties, state machine, hooks, cross-cutting infrastructure (`_save_chat_history`, `_log`, notification sync, legacy tc_inbox drain), and pass-through stubs all live here. The package is not "thin shell + 6 leaves"; the remaining code is genuinely cross-cutting or bound to the class definition.
- **Pass-through pattern.** Each extracted method becomes a 2-line stub in `__init__.py` (lazy import + call). This preserves the `BaseAgent` class interface while the implementation lives in submodules. The `self` → `agent` conversion is mechanical but `_heartbeat_loop` (183 lines, 15+ cross-cluster calls) deserves extra-careful review.
- **Subclass overrides stay on `__init__.py`.** `_activate_preset`, `_activate_default_preset`, `_build_launch_cmd`, `_pre_request`, `_post_request`, `_on_tool_result_hook`, `_cpr_agent` are all overridden by the `Agent` subclass in the wrapper package. They must remain as methods on `BaseAgent`.
